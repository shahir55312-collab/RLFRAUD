[project]
name = "rl-agent"
version = "0.1.0"
description = "RL fraud detection agent"
dependencies = [
    "fastapi",
    "uvicorn",
    "numpy",
    "gymnasium",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:app"
